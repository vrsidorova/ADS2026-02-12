package by.it.group510902.vinnichuk.lesson02;

import java.util.ArrayList;
import java.util.List;
import java.util.Arrays;
/*
Даны события events
реализуйте метод calcStartTimes, так, чтобы число включений регистратора на
заданный период времени (1) было минимальным, а все события events
были зарегистрированы.
Алгоритм жадный. Для реализации обдумайте надежный шаг.
*/

public class A_VideoRegistrator {

    public static void main(String[] args) {
        A_VideoRegistrator instance = new A_VideoRegistrator();
        double[] events = new double[]{1, 1.1, 1.6, 2.2, 2.4, 2.7, 3.9, 8.1, 9.1, 5.5, 3.7};
        List<Double> starts = instance.calcStartTimes(events, 1); //рассчитаем моменты старта, с длинной сеанса 1
        System.out.println(starts);                            //покажем моменты старта
    }

    //модификаторы доступа опущены для возможности тестирования
    List<Double> calcStartTimes(double[] events, double workDuration) {
        //events - события которые нужно зарегистрировать
        //timeWorkDuration время работы видеокамеры после старта
        // Создаю список для хранения моментов включения камеры
        List<Double> result = new ArrayList<>();

        // Первым делом сортирую массив событий. Это мой «надежный шаг»:
        // чтобы жадно выбирать моменты старта, мне нужно идти строго по порядку времени.
        Arrays.sort(events);

        int i = 0;                              // i - это индекс текущего события в отсортированном массиве
        int n = events.length;                  // Общее количество событий для удобства

        // Запускаю основной цикл, который будет работать, пока не обработаю все события
        while (i < n) {

            // Выбираю самое раннее из еще не покрытых событий — это и будет момент старта камеры.
            // Жадный выбор: нет смысла включать камеру раньше самого первого доступного события.
            double startTime = events[i];
            result.add(startTime);

            // Вычисляю время, когда камера закончит запись (текущий старт + длительность работы)
            double stopTime = startTime + workDuration;

            // Теперь мне нужно «проглотить» все события, которые успели попасть в этот интервал записи.
            // Пока время следующего события меньше или равно времени окончания работы камеры,
            // я просто увеличиваю индекс, так как эти события уже зарегистрированы.
            while (i < n && events[i] <= stopTime) {
                i++;
            }

            // Как только цикл встретит событие, которое позже stopTime, внешний цикл
            // снова создаст новую точку старта для этого следующего события.
        }

        return result; // Возвращаю итоговый список всех моментов включения
    }
}
                                   //i - это индекс события events[i]
        //Комментарии от проверочного решения сохранены для подсказки, но вы можете их удалить.
        //Подготовка к жадному поглощению массива событий
        //hint: сортировка Arrays.sort обеспечит скорость алгоритма
        //C*(n log n) + C1*n = O(n log n)

        //пока есть незарегистрированные события
        //получим одно событие по левому краю
        //и запомним время старта видеокамеры
        //вычислим момент окончания работы видеокамеры
        //и теперь пропустим все покрываемые события
        //за время до конца работы, увеличивая индекс



